output_root = "/path/to/data"
bin = "/path/to/decohered-cft/monte-carlo-simulation/target/release/decohered-cft"

lx = 8
lt = 8
seed = 2254
samples = 1000
chunks = 10

# One campaign may contain either or both noise types and any number of p values.
# Values are whitespace-separated inside the quoted strings.
noises = "z zz"
p_values = "0.00 0.05 0.10 0.15 0.20 0.25 0.30 0.35 0.40 0.45 0.49"

update = "corrected-wolff"

clean_therm_sweeps = 10000
clean_skip_sweeps = 100
disorder_therm_sweeps = 2000
measurements = 100
skip_sweeps = 20

partition = "day"
generate_time = "00:30:00"
generate_mem = "16G"
generate_cpus = 1
measure_time = "2:00:00"
measure_mem = "32G"
measure_cpus = 8
merge_time = "00:30:00"
merge_mem = "16G"
merge_cpus = 1
