[package]
name = "decohered-cft"
version = "0.1.0"
edition = "2024"

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
panic = "abort"
debug = "line-tables-only"
strip = "symbols"
incremental = false

[dependencies]
clap = { version = "4.6.1", features = ["derive"] }
rayon = "1.12.0"
